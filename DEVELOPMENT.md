# 项目最终目标

开发一个供机器人控制使用的功能完善的 EtherCAT 主站。

- **机器人控制**：主站接收外部控制器的实时位置指令，周期性发送给 EtherCAT 从站，读取反馈并提供给外部控制器
- **功能完善**：满足实时性要求、支持多轴同步、处理状态管理和故障检测、提供监控和安全机制

---

# 开发要求

## 工程性规范

### 代码组织与持久性
- 不临时性：代码和配置是持久的、可维护的，不创建临时脚本或测试文件
- 中文注释和说明：代码注释使用中文，Git 提交信息使用中文说明

### 解耦
- 模块职责清晰，按功能划分，避免跨层依赖
- 不与具体主机绑定：不硬编码主机名、路径、网卡名称
- 不与具体路径绑定：使用相对路径或配置文件，不依赖特定目录结构
- 配置驱动：参数通过配置文件管理，代码不包含硬编码的业务参数

### 启动与使用
- 启动命令简洁，不堆砌参数
- 通过配置文件或约定（如主机名）选择运行参数
- 不在命令行传递大量参数

### 验证与文档
- 实机测试验证：通过真实硬件验证功能
- 不维护测试代码：不创建独立的测试程序
- 不增加测试文档：不编写专门的测试说明文档

### 版本管理
- Git 版本管理：所有代码和配置提交到版本库
- 提交信息清晰：说明改动内容和原因

## 原子问题与执行顺序
- 将任务分解为原子问题（不可再分的最小问题单元）
- 执行顺序不强制、不固定
- 必须符合依赖逻辑：问题 A 依赖问题 B，则先解决 B
- 无依赖关系时，以功能性优先

## 正向功能优先
- 先实现理想环境下的完整功能
- 关于错误、冲突、边界情况的处理，除最基本的（也是最重大的），应该放在理想环境的"完成"后面实现
