diff --git a/include/emaster/bus/preop_probe.h b/include/emaster/bus/preop_probe.h
index bfa216d..15b3019 100644
--- a/include/emaster/bus/preop_probe.h
+++ b/include/emaster/bus/preop_probe.h
@@ -61,6 +61,8 @@ typedef struct
     uint16_t state;
     bool has_dc;
     bool has_coe;
+    /* P4.2: 记录从 SII 读取的 mailbox read length，用于判断 ecx_mbxhandler 推进上限 */
+    uint16_t mbx_rl;
     size_t sdo_read_count;
     emaster_sdo_read_t *sdo_reads;
     /* 当前生效 PDO 映射由分配对象逐层只读发现，不由设备配置预填。 */
diff --git a/src/bus/soem/preop_probe.c b/src/bus/soem/preop_probe.c
index fe667b2..192f04d 100644
--- a/src/bus/soem/preop_probe.c
+++ b/src/bus/soem/preop_probe.c
@@ -169,6 +169,7 @@ static bool capture_slave(ecx_contextt *context, uint16_t position,
     result->state = slave->state;
     result->has_dc = slave->hasdc != 0U;
     result->has_coe = (slave->mbx_proto & ECT_MBXPROT_COE) != 0U;
+    result->mbx_rl = slave->mbx_rl;
     result->sdo_read_count = request_count;
     /* 每个从站拥有独立结果数组，由报告析构函数统一释放。 */
     result->sdo_reads = calloc(request_count, sizeof(*result->sdo_reads));
diff --git a/tools/fingerprint/fingerprint_json.c b/tools/fingerprint/fingerprint_json.c
index c00c164..bc089a2 100644
--- a/tools/fingerprint/fingerprint_json.c
+++ b/tools/fingerprint/fingerprint_json.c
@@ -342,11 +342,12 @@ int emaster_fingerprint_write_json(FILE *output, const emaster_preop_report_t *r
         fprintf(output,
                 ",\"sii_identity\":{\"vendor_id\":%u,\"product_code\":%u,"
                 "\"revision\":%u},\"state\":%u,\"has_dc\":%s,\"has_coe\":%s,"
-                "\"target_profile_id\":",
+                "\"mbx_rl\":%u,\"target_profile_id\":",
                 (unsigned int)slave->identity.vendor_id,
                 (unsigned int)slave->identity.product_code,
                 (unsigned int)slave->identity.revision, (unsigned int)slave->state,
-                slave->has_dc ? "true" : "false", slave->has_coe ? "true" : "false");
+                slave->has_dc ? "true" : "false", slave->has_coe ? "true" : "false",
+                (unsigned int)slave->mbx_rl);
         json_string(output, expected->profile_id);
         fprintf(output, ",\"target_profile_match\":%s,\"target_pdo_match\":%s,"
                         "\"pdo_layout\":{\"status\":\"complete\",\"rx\":",
