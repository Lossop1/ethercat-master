# 分层计划与原子问题排序

2026-09-11 建立，2026-09-16 改版为「未决清单」。

依据 `capability-baseline.md`。分层原则：**自底向上，下层不知道上层存在。**
每一层只向下依赖，向上只提供"数据点 + 能力位图 + 时序契约"三件，不提供语义解释。

---

## 0 本文怎么用

老版本停更在 2026-09-14。此后 22 个提交（停机序言收尾、与设备解耦、观测通道、工程化）
一件都没进来。问题不在纪律，在结构：全文 288 行里 30 条有 27 条已完成，完成记录的正文
占了近一半，越写越像日志，于是没人再从头看。2026-09-16 改版定下四条规则：

1. **本文只装未决问题。** §2 是唯一的未决入口。做完的移出 §2，一行进 §4 索引，
   细节正文进 [`layering-plan-archive.md`](layering-plan-archive.md) ——
   完成记录本来就由 commit log 承担，不必在这里复述一遍。
2. **编号只增不复用。** 代码注释直接引用 P 编号（`session_exchange.c` 引 P4.4、
   `session_start.c` 引 P4.3），编号必须永久可查，所以已解决的要留在 §4 索引里。
3. **只有"有客观完成判据"的才进 §2。** 策略选择进 §3「待决」，它的判据是
   "做决定所需的证据齐没齐"。混进 §2 会把"完成判据"列变成空话 ——
   而正是这一列，让 P0–P7 能被一条条判完。
4. **每轮会话收尾时**，把当轮新发现的问题登记进 §2 或 §3。

---

## 1 层级划分

| 层 | 名称 | 职责 | 明确不做 | 时序等级 |
| --- | --- | --- | --- | --- |
| L0 | 主机实时基座 | RT 线程、内存锁定、CPU 隔离、周期时钟、死区判定 | 不知道 EtherCAT | 周期内 |
| L1 | EtherCAT 传输 | 帧收发、FMMU/SM、ESM、DC、WKC、错误计数器、邮箱调度 | 不知道 CiA402，不知道"轴" | 周期内 + 跨周期 |
| L2 | 过程映像与对象访问 | 字节偏移 ↔ 命名数据点；SDO 读写（非 RT 线程） | 不解释数据点含义 | 周期内 + 非实时 |
| L3 | 设备语义（CiA402 轴） | 状态机、控制字/状态字、模式、原始 counts 进出 | **不做单位换算**，不知道减速比 | 周期内 |
| L4 | 运动与安全 | 单位换算、限幅、插值、跟随误差、包线、周期内控制律（PD/阻抗） | 不知道命令从哪来 | 周期内 |
| L5 | 命令与观测接口 | 目标下行、观测上行、失效超时、双缓冲 | **不构造观测帧**，不知道上层是 RL 还是示教 | 跨周期 |
| L6 | 上层应用 | 策略推理、帧构造、任务编排 | 不在本仓库 | 上层自定 |

### 1.1 边界判据

一条功能属于哪层，看它**需要知道什么**：

- 需要知道减速比/编码器分辨率 → L4 及以上（L3 只搬 counts）
- 需要知道"这是一条腿的髋关节" → L6
- 需要知道帧格式/传输方式 → L5 以上
- 需要知道 SM/FMMU → L1

### 1.2 L4 必须承载周期内控制律

这不是设计偏好，是硬件逼出来的：`MIT_Control`（kp/kd + 前馈力矩）在 EtherCAT ESI 中
无对应对象（见基线 2.4）。若上层需要关节阻抗，PD 环只能放在 L4 按周期跑，
输出折算为 CST 力矩或 CSP 位置。L4 因此不是纯粹的"限幅层"。

### 1.3 L5 的形态由时序反推，不预设

已知量：EtherCAT 周期 1ms；典型策略频率 50-100Hz（rl_sar `config.yaml` 量级）。
⇒ 周期比在 10:1 到 20:1，**延迟不是瓶颈**。

真正的约束只有两条：

1. RT 周期**绝不能阻塞等待** L5 ⇒ 排除同步请求-响应
2. 上层命令缺失或迟到时必须安全降级 ⇒ 必须有失效超时（当前已实现）

满足这两条的实现：Unix 域套接字 + 互斥保护双缓冲（已实现）。

观测量按可得性分两档（硬件决定，非设计选择）：

- **周期档**：位置、速度、力矩（在 TxPDO 内，见基线 2.2）
- **慢速档**：电流、电压、温度（只能 SDO，约 3 周期 + 抖动，见基线 2.3）
  ⇒ 10-100ms 级轮询，错开轴号

IMU 不在 EtherCAT 链路上，本轮不纳入。

---

## 2 未决的原子问题

### P8 运行期时序与可靠性

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P8.1 | **整帧未回**：运行期每隔约 45 秒有 1–3 个周期收不到任何回帧。**已定性为超时偏紧造成的误报为主**（250 µs 固定超时 vs 实测成功回程上限 170–390 µs，见下）；是否还另有真丢帧未定 | 连续 ≥1 小时，`wkc_no_frame_count` 的**每个**事件都用非超时证据归因（发送迟到 / ESC 计数器 / AL 状态 / DC 裕量），且期间无掉出 OP（AL 全程 8）；报告须同时给出 `tail_max_receive_ok_ns`，使"其中多少是 250 µs 偏紧造成的误报"可估。**不要求计数为 0**——超时固定在 250 µs 而成功回程实测到 390 µs，为 0 是个不可达且无意义的门槛 | 未决；超时已固定，误报成分已知，待一小时归因 |

**P8.1 已有结论（2026-09-17）**

现象计在 `wkc_no_frame_count` 上，与"帧回来了但 WKC 少计"的 `wkc_mismatch_count` 分开。
四项排除已经做完：

- **与观测通道无关**：8 臂隔离试验里，观测通道全关的 `base` 臂反而最多（3、3 次），
  开通道但一个客户端都没有的 `obsnone` 臂是 0、0、4 次。此前把故障归因于观测通道的判断
  **已撤回**。
- **不是链路问题**：5 个从站的 ESC 错误计数器全程为 0（`rx` / `forwarded_rx` / `epu` / `pdi`）；
  首次不符现场快照里帧距 1.0005 ms（发帧准点）、轴仍在 AL state=8（没有先掉出）。
- **不是发送晚**：8 臂的 `max_send_lateness` 全程 ≤55 µs（最大 55488 ns），发送侧不构成嫌疑。
- **主因是超时压在回程分布尾部**——不是"帧没回来"，是"回来得比超时晚"。见下。

判定主因用的 8 臂数据（五轴，各 45 s，前 4 臂超时 250 µs、后 4 臂 600 µs）：

| | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `wkc_no_frame_count` | 3 | 1 | 1 | 3 | 2 | 1 | 0 | 0 |
| `max_send_lateness` (ns) | 1323 | 25795 | 23956 | 26056 | 21230 | 3689 | 35231 | 55488 |
| `tail_max_receive_ns` | 267893 | 268814 | 274054 | 268974 | **645525** | **644194** | **222045** | **178196** |
| `sync0_late_count` | 16 | 57 | 15 | 67 | 84 | 0 | 0 | 0 |

三条读数：

1. **未回周期的收包段时长恒等于超时值本身**（250 组 = 268–274 µs ≈ 250 + 收尾开销；
   600 组的失败臂 = 645/644 µs ≈ 600 + 收尾开销）。这是"等待被超时截断"的指纹，
   不是"帧丢了"。
2. **干净臂的收包段上限只有 178–222 µs**，而超时是 250 µs —— 余量仅 12%。
   超时从一开始就压在分布的尾部上。
3. 超时 250 → 600 µs 后计数 8 → 3，是剂量-反应。

**修复尝试与撤回（2026-09-17）**

先按"超时压在尾部"改了（提交 `aafff14`）：`frame_timeout_us` 由实测 `tail_max_receive_ok_ns × 2`
导出、钳在 `[cycle_ns/4, cycle_ns/2]`；FPRD 诊断读拆出独立的 `fprd_timeout_us`；标定样本
只收 `actual_wkc > 0` 的周期。**取值方式当日撤回**（`fprd_timeout_us` 的拆分保留）。理由：

实测上限是**全程最大值、只涨不落**，而超时又等于它的 2 倍。于是一个大样本就把超时顶上去，
此后的慢帧全被判成"回来了"——`wkc_no_frame_count` 被它自己压成 0。**这不是链路变好，是把
尺子放宽**；拿这个数字当判据，判据就是不可证伪的。

收手的判断依据：掉线主因已在别处定位并修好（停机序言），这些未回事件从未引发掉线。
肥尾按"非 PREEMPT_RT 内核 + 台架共存负载的固有属性"记录，不再试图用超时盖掉。
`tail_max_receive_ok_ns` / `tail_max_receive_ns` 的测量与上报**保留**——超时不动了，
"实测离超时还有多远"才是一个有意义的比较。

**5 臂实测（五轴，各 45 s，`base`）**

| 臂 | `tail_max_receive_ok_ns` | `frame_timeout_us`（自适应版） | 撞 `cycle_ns/2` | `sync0_late_count` | `wkc_no_frame_count` |
| --- | --- | --- | --- | --- | --- |
| 1 | 390 µs | 500 | 是 | 2 | 0 |
| 2 | 311 µs | 500 | 是 | 1 | 0 |
| 3 | 171 µs | 340 | 否 | 63 | 0 |
| 4 | 181 µs | 360 | 否 | 0 | 0 |
| 冒烟 | 174 µs | 346 | 否 | 47/48 | 0 |

**由此确认的两件事**

1. **`sync0_late_count` 与尾部不相关，上面那条候选解释不成立。** 回程上限最小的臂
   （171 µs）迟到 63 次，上限最大的臂（390 µs）迟到 2 次——方向与"5 轴同时 SYNC0 压住
   ESC 转发造成肥尾"相反。原先的依据（"有未回的臂 84/67/57/16、干净臂 0/0"）在新数据下
   不成立：干净臂的迟到计数可以是 0、1、2、47、63，与未回计数无关。
2. **报告里的 `round_trip_ns` 分布不能用来判超时余量。** 它从发帧算到 3×FPRD 之后
   （`session_exchange.c:495` 的 `receive_end`），量的是**整个周期尾部**（收包 + 邮箱 +
   诊断读），不是帧的回程。判余量要看 `tail_max_receive_ok_ns`（纯收包段，
   `session_exchange.c:412`）。8 臂那会儿用 `tail_max_receive_ns` 是对的，没有踩这个坑。

**已知读数缺陷（不是新问题，是重新确认的旧问题）**：`cycle_ns/4` = 250 µs 确实偏紧
（实测成功回程上限 170–390 µs），所以 `wkc_no_frame_count` 里**含一个误报成分**——那些帧
回来了，只是回得比 250 µs 晚。判读该计数时必须扣掉这一层，不能直接当成"帧丢失"。
是否还另有真丢帧，是 P8.1 剩余待分辨的部分。

### P8.5 结案记录（已解决，索引见 §4；下面的取证过程留在原处，只读）

**P8.5 已取到的证据（2026-09-17，五轴 60 s CSP 往返两轮：temp60 = 旧二进制 + 5 秒保持窗口，alprobe = `df0162e` + 不保持）**

两轮运行期都干净：alprobe 客户端 1 秒采样显示五轴跟目标到 t+59 s，`wkc_error_count=4`
（正好是停机那四拍）、`wkc_no_frame_count=0`、`soem_errors=0`、`first_cycle_failure=null`。
掉出全部发生在**停机的瞬间**，且两轮都在停机入口读到**五轴 AL 20/0x1A**
（更早三轮长跑只掉 DC 参考从站，是本部署的既有现象，与本条无关）。

| | 停摆那一拍（`tail_uncovered`） | 下一拍帧距 | 停机首拍 WKC | 停机入口 AL |
| --- | --- | --- | --- | --- |
| alprobe | 交换 75804：4.17 ms（该拍自身各段正常：发 3.3 µs / 收 132 µs / 邮箱 0.14 µs / 计数器 0.04 µs） | 交换 75805：**5.018 ms** | 5（= 只剩 5 个从站的输入计入） | 五轴 20/26 |
| temp60 | 交换 80799：3.79 ms | 交换 80800：**4.014 ms** | 5 | 五轴 20/26 |

三条读数：

1. **因果方向是"先停摆、后掉出"**：停摆拍的 WKC 还是 15（五轴都在计），掉出只出现在
   停摆之后的那一拍，且此后连续四拍都是 5。设备端的容差落在 2～3 ms 之间——更早三轮长跑
   的帧距上限是 2.0 ms（且发生在启动段），那三轮停机入口是四轴 8/0。
2. **停摆不在已计时的段里**：该拍的发 / 收 / 邮箱 / 诊断读四段合计 < 0.2 ms，停摆落在
   `tail_uncovered`（收包之后到探针之间：timing 统计、现场入环、逐轴 PDO 审计输出）。
   同样的代码每拍都跑，只有这一拍慢 4 ms ⇒ 嫌疑人不是这段代码本身，而是**周期线程被拿走**
   （调度/锁），与既有记录"管理面轮询照样打死周期线程"同类。触发时刻与脚本发 SIGINT 重合。
3. **内联序言确实做到了它承诺的事**：`first_safe_frame` 距停机原点 0.999 ms（= 一个时钟栅格），
   缺口不是问题；问题是**停摆没有被消除，只是从"帧与帧之间"挪进了"某一拍的尾部"**。

控制点（同日 CST 轮，停机由驱动器故障触发而非 SIGINT）：同一段内联序言跑了 4 拍 / 4.00 ms，
停机那一拍帧距 1.00 ms，最后 11 拍帧距全在 0.995～1.005 ms，停机入口五轴 AL 全是 8/0
——**停摆不是内联序言固有的**，是"停机请求落地那一刻"（SIGINT 或其它外部动作介入）特有的。

**P8.5 历史回扫（2026-09-17 补，台架上五轴归档报告 20 份全部扫过）**

上面那条"更早的长跑是干净的"只对了一半。同一个指纹——尾部停摆 3.8～4.2 ms → 下一拍帧距
4.0～5.0 ms → 停机首拍 WKC 5 → 停机入口五轴 20/26——在**六份**归档里都在：

| 报告（归档名后缀） | 周期数 | 尾部停摆（拍） | 下一拍帧距（拍） | 停机入口 |
| --- | --- | --- | --- | --- |
| `20260915T203326Z-verify7` | 201361 | 3.78 ms（201357） | 3.97 ms（201358） | 五轴 20/26 |
| `20260915T203948Z-probe9` | 111275 | 4.10 ms（111271） | 5.02 ms（111272） | 五轴 20/26 |
| `20260915T204649Z-probe10` | 110772 | 4.18 ms（110767） | 5.00 ms（110768） | 五轴 20/26 |
| `20260915T221116Z` | 218581 | 4.12 ms（218576） | 5.01 ms（218577） | 五轴 20/26 |
| `…082032Z-temp60` | 80804 | 3.79 ms（80799） | 4.01 ms（80800） | 五轴 20/26 |
| `…083338Z-alprobe` | 75808 | 4.17 ms（75804） | 5.02 ms（75805） | 五轴 20/26 |

前四份早于 temp60/alprobe 两轮，且都已带内联序言（`inline_drain_cycles` = 4/4/5/5）。
⇒ **这条不是 09-17 才出现的新现象，是间歇复发的**；此前认不出来，是因为
`tail_uncovered` / `cycle_trace` 是这两天才加的仪表，"停摆落在某一拍尾部"在旧报告里看不见。

同一批归档里也有干净收尾的：`221721Z`（140407 拍，尾部上限 11.5 µs，五轴 8/0）
与五轮 57～58 s 的运行（尾部 ≤12.5 µs，五轴 8/0）。**同样的代码、同样的入口，一半的运行会掉、
一半不会**——差别不在配置，在停机那一拍的处境。

剂量-反应还有一个中间点：`193228Z`（105996 拍）尾部停摆 1.48 ms → 帧距 2.0 ms，
**只有轴 3 一轴掉出**（其余四轴 8/0），与"设备容差落在 2～3 ms"一致。
部分轴掉出的还有：`221145Z`（轴 5）、`060845Z`（轴 2 + 轴 5）、`062421Z`（轴 5）、
`070949Z`（轴 3 + 轴 5，其中轴 3 是在序言内部掉的）、`074924Z`（轴 1/4/5，1222 拍即终止）。

**停摆的归属又窄了一步（2026-09-17，代码核对）**：SIGINT 路径只做两件事——信号处理器写一个
`volatile sig_atomic_t`（`tools/master/main.c:41-46`），周期循环每拍在安全闸门里查一次
（`session_supervisor.c:117-118`，由 `session_control.c:657` 调用）。而停摆落在**交换函数的
尾部**（`session_exchange.c:835-855`），在"查标志"之前——**停摆不是"处理停机请求"这个动作本身
造成的**，处理器只是一个存储操作；候选回到"那一拍自身在干什么"与"被谁抢占"两支。
另一个容易误读的地方：退出循环的那一拍只把 SAFE_STOP 控制字写进输出缓冲，**帧并没有发出去**；
真正的第一条安全停机帧由停机阶段的 `stop_process_data` 发出，这才是缺口测量的语义起点。

**P8.5 停摆归属的排除法（2026-09-17 第二轮代码核对 + 台架环境取证，未动设备）**

上一节把停摆定位到"交换函数尾部未覆盖段"。这一轮逐条读代码 + 读台架环境，把四类候选**排除**：

| 候选 | 排除依据 |
| --- | --- |
| 缺页 / 内存回收 / THP 压缩 | 台架 32 GB 内存、18 GB 空闲、无交换；`compact_stall` / `compact_fail` / `allocstall_*` / `pgscan_direct` / `pgsteal_direct` **自开机以来全为 0**；THP 虽为 `always`，但 `thp_fault_fallback` = 0（从未退化分配）；CPU 11 调速器为 `performance` |
| 尾部审计追加触发扩容 | 审计容量在会话开始前**一次性算出、预留并预触及**（`session_observer.c` 的 `emaster_session_observer_prepare_audit`：`reserve` 里 `memset` 新增段，随后 `seal_capacity`）。封存后 `append_access` 只计数、不再分配。**顺带解释了一个此前没解释的读数**：多份长跑报告体积都卡在 312–331 MB，那是封存容量，不是运行时长 |
| 管理面锁把周期线程挡在尾部 | 尾部未覆盖段调用的三个文件（`src/audit/`、`src/cyclic/timing.c`、`cia_process_image.c`）里 `pthread_mutex*` / `sem_wait` / `pthread_cond*` 出现 **0 次**；全仓只有 `command_server.c` / `session_control.c` / `soem_common.c` 用锁，都不在这条链上 |
| EtherCAT 网卡中断打在绑核上 | 网卡 32 个 MSI-X 向量中只有 `enp49s0-21` 有计数（16 次），全部落在 CPU 9；CPU 11 列全 0。RT 节流为内核默认 950000/1000000 µs，周期线程占用约 2.5%，够不着 |

排除之后剩下的只有一支：**停摆期间周期线程不在 CPU 上执行自己的代码**。尾部那三件事本身是纳秒级的纯内存操作；
线程既没有阻塞调用可睡（无锁、无 I/O、无分配），同优先级的另外两个 FIFO 线程（观察、命令）也**不能抢占**它
（`session_shutdown.c` 里对这条同优先级假设有专门注释）。所以能把它从 CPU 11 上拿走的，只剩
**硬中断 / 软中断（在中断上下文里跑，不受 RT 抢占约束）/ 更高优先级的 RT 线程（线程化 IRQ 默认 FIFO 50）**。

两条需要一起验证的线索（本轮都还没有证据，只是候选）：
一是停摆时刻与"运行收尾"重合——客户端断开、脚本的 `nc` shutdown 新连接、SIGINT 挤在几毫秒内，它们各自会在**命令线程**
（同样 FIFO 19、同样绑 CPU 11）上引发套接字收尾工作，内核侧随之产生的收尾（socket 释放、RCU 回调）落在哪个 CPU 上未测；
二是 CST 轮（停机由驱动器故障触发，没有这套收尾动作）没有出现过停摆——与控制点的那条记录方向一致，但样本只有 1。

**下一步的最小代价做法**（判据已写进表中）：不要再加分段计时点，那只会在同一维度上切得更细。要加的是
**墙上时间与线程 CPU 时间的差**，以及非自愿切换计数；外加**不带运动目标**的复现轮（客户端把当前位置当目标发，
命令流形状与真往返一致、轴不转），以便在同一形状下反复取数。

**P8.5 第三轮：带内核记录跑真机（2026-09-17，未取得 P8.5 数据）**

用户批准"直接带内核记录跑 30° 往返"后按计划开记录（只抓 CPU 11：`sched_switch` /
`sched_waking` / 硬中断 / 软中断，128 MB 缓冲，`mono` 时钟）。**主站这一轮只活了 4.0 秒就
自行退出，没走到稳态循环**：

| 时刻（mono） | 事件 |
| --- | --- |
| 25426.4 | 开始记录（17:29:04） |
| 25428.5 | 主站进程起来（17:29:06） |
| 25432.5 | 主站自行退出，退出码 1（17:29:10） |
| 25491.9 | 停记录（17:30:10）；中间那 60 s 是台架脚本对着已死的套接字轮询 `state`（脚本的 60 次等待） |

失败点在**使能**：五轴 `6041` 停在 0x1250（轴 1/2，解码 = Switch on disabled）与 0x1231
（轴 3/4/5，解码 = Ready to switch on），控制字在 0x0006 / 0x0007 之间交替——主站的使能时序
在等轴 1/2 走到 ready to switch on，它们没走；日志 `全部运行使能到达=失败`、`运动开始=失败`。
**没有任何运动，客户端一次也没跑。** 同一轮里交换号 1210 处帧距 2.0 ms
（`frame_interval_max_ns=2000331`），轴 1/2 的 AL 掉到 20/0x1A，到 1214 拍 WKC 11≠15 终止；
`tail_uncovered_max_ns=232535`（0.23 ms）、`over_budget_cycle_count=0` ⇒ **这不是 P8.5 的
4 ms 尾部停摆，是"启动/使能期的整拍缺失"**，与 `193228Z` 那一类同族（同样 2.0 ms 帧距，这次掉
两轴）。停机仍是一帧安全输出都没发（`safe_output_sent=false`），与 D2 的既有结论一致。

**这一轮唯一的正面产出，是判据表要的那两栏第一次落了数**：报告 `thread_schedstat` 给出周期线程
（tid 83588）运行 0.074 s、**排队等待 0.048 ms**、切换 18599 次；内核记录独立算出同一线程
18608 次上 CPU、跨度 3.97 s、**相邻两次上 CPU 的最大间隔 38.4 ms**（落在刚启动那一拍），
超过 1 ms 的间隔 1807 个（集中在使能阶段，与"1214 拍耗掉 4 秒"一致）、超过 5 ms 的 4 个
（全部落在启动与收尾）。⇒ **在这个窗口里"CPU≈0 且排队等待≈0"，线程长时间不在 CPU 上属于
"自己阻塞"，不属于"被拿走"**；这与排除法剩下的那支（硬中断 / 中断上下文的软中断 / 更高优先级
RT 线程）不矛盾——只是这一窗口没发生那类抢占，P8.5 的那一拍仍未取到。

**工具口径坑（记下来免得再踩）**：ftrace 文本记录里的时间戳是**秒**（`秒.微秒`，小数部分也是秒），
自己拿它算间隔时乘 1000 得到的是**毫秒**；
乘 1000 得到的是**毫秒**；第一版把它标成 µs，于是 38.4 ms 被读成 38.4 µs，再和"3.97 秒跨度、
1807 个间隔大于 1 ms"对不上，白追了一轮。已一律改成乘 1e6，阈值也按 µs 给。教训：
**同一份输出里出现"两个都可信、又互相矛盾"的数字时，先怀疑自己的单位，不要怀疑数据。**

阻塞：轴 1/2 拒绝使能不解决，就没有 P8.5 的复现轮——见下面 P2.6。

**P8.5 第四轮：根因锁定（2026-09-17，五轴 180 s / 位移 0 度 / 带内核记录，TAG=p85b）**

方法：在周期线程的绑核（CPU 11）上，除原有的 `sched_switch` / `sched_waking` / 硬中断 / 软中断外，
再加 **`raw_syscalls:sys_enter|sys_exit`**（任何一个系统调用都会留下号与时长）和**四个用户态探针**
（uprobe，按 ELF 文件偏移注册：交换入口 / 计时统计 / 审计输出 / 命令接收）。**这一轮不带动运动目标**
（`--degrees 0`，客户端仍按 100 Hz 发目标、命令流形状与真往返一致，轴不转）。

结果：**掉线复现**。`tail_uncovered_max_ns=3789298`（3.789 ms，交换 201231）、
`frame_interval_max_ns=4025832`（4.026 ms，交换 201232）、`first_mismatch_exchange=201232`、
`cycle_count=201235`、停机入口五轴 AL 20/0x1A——与归档里那一族逐项同形。

**定位过程（每一步都是记录直接给的，不是推断）**

1. 32.7 s 的记录里，交换入口到交换入口的间隔**只有一个**超过 2 ms：4.755 ms（27873.068258 → 27873.073013）。
2. 停摆那一拍，周期线程（tid 95075）在 CPU 11 上**一次连续占用 3.811 ms**：没有被换出、没有抢占、
   期间 CPU 11 上没有任何别的线程在跑（只有 2 个 arch_timer 中断和 1 个软中断）。⇒ **不是"被拿走"。**
3. 这 3.811 ms 里线程耗在一个系统调用上：**号 216 = `mremap`，耗时 3.757 ms**。
   整个 32.7 s 记录里 `mremap` 只出现这一次，`mmap` 一次都没有。
4. 探针把它的位置夹死了：在第 1 个轴的"审计输出"入口之后 **2 µs** 进 `mremap`，3.757 ms 后返回，
   再跑其余 4 个轴的审计输出。⇒ 这是**审计记录追加时的一次扩容**（`append_access` 的 `realloc`，
   glibc 对大块走 `mremap`）。

**为什么以前把它排除掉是错的**

周期阶段确实是"预留 + 封存"的，而且设计意图完全生效了：`cyclic_capacity=960895`，数组在**交换
175710 拍**填满（记录里最后一条的 `last_exchange`），此后周期阶段一条都不再分配、只计数
（`omitted_pdo_samples=96276`，占最后 25525 拍、约 3.8 条/拍）。所以"周期阶段不会分配"这句是对的。

漏掉的是**停机序言会把封存解开**——`session_shutdown.c:835` 调 `emaster_run_audit_end_cyclic`，
注释写得很清楚：不解封则"安全停机阶段恰恰是故障发生的阶段，一个样本都留不下"。解封那一刻数组**正好是满的**
（已记录数 == 容量），于是**停机后第一条记录**就必须扩容：84.6 MB 的块翻倍，内核搬页表花了 3.757 ms。

**因果链（与归档指纹逐条吻合）**

停机请求落地 → 序言解封审计数组 → 第一条 PDO 审计记录触发扩容 3.757 ms（正好落在周期尾部）→
下一帧晚发 4.026 ms → 五轴同步看门狗到点 → AL 0x1A / WKC 5（交换 201232 首次不符）。
归档里"停摆总在最后 4–6 拍"因此不是巧合、也不是"运行结束导致的"——**它本来就是停机入口那一拍**。
上一轮记录的"因果倒置"假设（停摆终止了运行）**作废**；原始相关性（停机触发停摆）才是对的。

**同拍的一条未定性观察**：tid 95086（不在报告的两条线程记录里）在停摆开始的同一瞬间走完
`write` + `exit`（系统调用 93）。它与停机请求谁先谁后没有定死。不影响上面的链条：
`mremap` 只可能来自解封（封存期间 `append_access` 不可能分配），而解封只发生在停机路径里。

**工具口径（下次直接用）**：`available_tracers=nop`（没有 function_graph / irqoff），能用的是事件 +
uprobe（`kprobe_events` 缺失、`uprobe_events` 可用）；`raw_syscalls` 的格式是 `NR 216 (...)`，
不是 `id=`。**`buffer_size_kb` 是每 CPU 一份**（读 `buffer_total_size_kb` 可看到它是乘过核数的总值），
没有固定的内核上限，能给多大由这台机器的内存决定；而且它有**按核**的写法
（`per_cpu/cpuN/buffer_size_kb`），只抓一核时就只加那一核，不必给 12 份都加大。
本轮这样的事件密度下 128 MB/核的环**只装得下最后约 27 s**（约 4.7 MB/s）——所以"记录起点"远晚于
"运行起点"是正常的，判据要按"最后若干秒"来找。寄存器偏移不能用 `nm` 的虚拟地址，要按 `PT_LOAD` 换算
（`scripts/analysis/uprobe_offsets.py`）。这一整套开记录 + 注册探针 + 收记录的流程已固化成
`scripts/bench_trace_cycle.sh`（`TAG`/`DURATION`/`DEGREES` 走环境变量），读记录用
`scripts/analysis/trace_busy_windows.py`（把"一次连续占用 CPU 超过 N ms"的窗口摊开、事件带人话标签，
判据是窗口长度——窗口够长说明 CPU 一直在自己手里，慢的原因在窗口内；窗口短而中间有大片空白才是被拿走）。

### P11.1 结案记录：修复与验证（2026-09-17 第五轮）

**改了什么**：审计数组从"一段容量"改成**两段**。会话建立时一次性预留总量（`final_capacity`，
谁都不许越过），并按本轮最大交换数 × 每拍字段数算出一条**周期段水线**（`sealed_capacity`）把它封住。
周期阶段写满水线就只计数（`omitted_pdo_samples`）；停机序言解封之后，写的是留给停机的那段差额
（水线 → 总量），**这段也用尽时同样只计数**（`omitted_final_samples`），不再分配。
即：**解封路径上不存在任何能走到扩容的分支**，容量在预留那一刻就锁死。

两笔溢出分开记，是为了从报告里一眼看出是"周期段被吃光"还是"停机预留也被吃光"。

**判据一（离线可证，已达成）**：新增 `tests/unit/audit/test_audit_capacity.c`，按故障那一轮的
调用次序复刻——预留 → 封存 → 写满水线 → 解封 → 写满差额 → 再追加。最后一步断言
`access_capacity` **原地不动**（容量不变即没有走扩容分支，这是"周期路径里不再分配"的硬证据；
修改前这里会翻倍，断言必红）。两个负对照：
把两处守卫都摘掉 ⇒ 9 项不符（含"总量用尽后容量必须原地不动"）；只摘掉"预留之前不得丢记录"
那一条的前提条件 ⇒ 恰好 2 项不符。

**判据二（台架，已达成）**：五轴 180 s 到自然结束（位移 0 度，客户端仍按 100 Hz 发目标、命令流
形状与真往返一致），带内核记录（`TAG=p11a`）：

| 指标 | 修改前（`p85b`） | 修改后（`p11a`） |
| --- | --- | --- |
| 停机入口五轴 AL | 20/0x1A（掉出 OP） | **8/0x0000** |
| 停机那一拍帧距 | 4025832 ns（4.026 ms） | **998826 ns** |
| 记录里 `mremap`(NR 216) | 1 次 3.757 ms | **0 次**（整份 3061281 行；这一版缓冲只覆盖最后约 27 s，停机那一拍在内） |
| 首次 WKC 不符 | 201232（终止） | 无（200744 拍自然结束，WKC 全程 15/15） |

报告 `audit` 段：`omitted_pdo_samples=81711`、`omitted_final_samples=0`、`access_count=961145`、
`cyclic_capacity=960735`、`final_capacity=961375` ⇒ **周期段按设计溢出并计数，停机预留一段不差地
用满，一条都没溢出**——正是修复想要的形状。

运行期另有**一次未级联的扰动**：交换 164009 处五轴 WKC 同时少计（各轴 AL 仍 8/0），下一拍即恢复，
没有像 P8.5 那样雪崩。它与停机无关，单次样本不足以定性，**不算已归因**。

**这一轮自己踩的坑（已修，留作回归网）**：给"解封后不许再分配"加预判时，写成
`access_count >= access_capacity`，而**预留之前**两边都是 0 ⇒ `0 >= 0` 成立 ⇒ 会话建立阶段
（PRE-OP 配置读写）的每一条记录都被当成溢出丢掉。台架上实测丢了 **255 条**配置轨迹（报告里
`omitted_final_samples=255` 而 `final_capacity=961120`——1210 拍的一轮不可能有这个数，是这么发现的）。
修法是给预判加上"**已经预留过**"这个前提（`final_capacity != 0U`），两侧记录路径同改；
单元测试新增"预留之前"一节把这条钉死。
教训：**给"满"下定义之前，先确认"空"的时候这个判据不成立。**

**没证到的**：判据二只有**一轮**干净运行 + 离线结构性证明，不是统计。`P11.2`（停机序言在
"部分轴已掉出"时是否吃逐轴超时）仍未决——本轮停机时五轴都在 OP，取不到那个情形。

### P2 / P5 的未决项（编号不变，承接 archive）

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P2.5 | Fault Reset 调用路径已存在但未真机验证 | `Fault → Fault Reset → Switch On Disabled` 全程可走 | 已实现·未验证（硬件阻塞） |
| P2.6 | **冷启动后轴 1/2 拒绝使能**：`6041` 停在 0x1250（Switch on disabled，电压使能位=1、故障位=0），主站使能时序等不到 ready to switch on；柜子断电重启后仍复现（2026-09-17 17:29 那一轮）。轴 3/4/5 正常走到 0x1231 | 冷启动后 60 s 内五轴全部到达 operation enabled（`6041` 位 2=1，即 0x1237 这一档），且报告里 `全部运行使能到达=通过` | 未决（台架侧，待用户在柜子上查驱动器面板与安全链：STO、急停回路、上电复位）。**2026-09-17 新数据：不是硬阻塞** —— 18:23 那一轮轴 2 停在 0x1250、其余 0x1231、WKC 13/15、只跑了 1210 拍；**6 分钟后同一套二进制、同一套配置、没有任何人工干预，五轴全部使能、WKC 15/15、跑到 200744 拍**。⇒ 该现象是**非确定性**的，重试本身就是一个可用的绕法；但触发条件仍未知（可能与上一次运行遗留的 AL 0x1A 状态有关，见 §3 D1 追加证据） |
| P5.1 | 运行期模式切换未实现（模式在计划构建期固定） | 0x119800 方案下 OP 态切换 CSP/CSV/CST | 未做（CSP 专项不要求） |
| P5.3 | `fixed_csv` 的 1A02 内容 ESI 与手册冲突，未验证 | 真机读回确认 | 硬件阻塞（CSP 专项不要求） |
| P5.4 | ~~无 3 轴及以上时序数据~~ **措辞已过时** | 逐步扩到目标轴数，每档留报告 | **待重判**：五轴配置已入库（`8e69b64`）、bench 脚本默认已改五轴，这条不能再挂"硬件阻塞" |

**P2.5 硬件阻塞原因**：驱动器静置后位置反馈正常但不执行 607A，断电重启恢复
（见 [[drive-idle-no-response]]）。

### P10 外部目标接口与控制台（2026-09-17 登记）

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P10.1 | 静止偏差未定量：面板上读到"已提交目标"与"实际位置"相差 0.04–0.18°，实际位置自身在小幅游走（±0.14°） | 给出静止段 \|已提交目标 − 实际位置\| 的上界，并判定来源（驱动器位置环刚度 / 编码器量化 / 负载反驱），数据取自 5 轴静止段而非目测 | 未决（现象来自面板读数，未定量） |
| P10.2 | **越限命令的语义与回执不一致**：软件限位或单步限幅不过 → `MOTION_INVALID` → `note_runtime_failure` → **整个会话中止**（`session_control.c:493-500`）；而套接字在**周期校验之前**就回了 `OK\|Updated N`（`session_control.c:990`，校验在 `session_target.c`），"OK"只代表写进缓冲 | 客户端不读主站日志就能区分"已接受"与"已被拒"（可轮询的拒绝计数 + 原因），且对"越限要不要打成整会话中止"给出结论：改就改，留就写下为什么 | 未决（对 RL 是硬约束：探索期一步给大了会话直接没） |
| P10.3 | 故障态停机的断供窗口未测：内联序言的判据是 `communication_usable()`（`session_shutdown.c:777`，故意不按"有没有锁存故障"），所以通信判不可用触发的停机仍走旧序言，2–3 ms 缺口回来 | 通信不可用触发的停机里，报告给出缺口实测值 + 其余轴 AL 轨迹；据此决定内联序言要不要在"部分轴掉出"时也生效 | 未决 |
| P10.5 | **CST 通道实况（2026-09-17，真机）**：模式 10 走通了——五轴 `6061` 回读 10（SAFE-OP 写后即读、OP 内读两处都过），五轴使能到 `6041=0x1237`，零目标时主站写 0 有日志与报告双重自证（`[MODE] Torque mode (CST): idle, outputting zero torque`、`target_torque=0`、`command_server.received_count=1` 说明全程没有任何客户端发过目标）。**但 CST 下位置环不工作：零力矩 ≠ 保持不动**——轴在 0 力矩下自由移动，轴 1 随即进故障态（`6041=0x1208`，fault 位），会话按"驱动器进入故障状态"中止 | 若重启这条线：先给出"零力矩段轴是否自由"的判据与机械防护（支撑/限位），加力矩改成本质安全的小步；**本轮 50 千分比（=5% 额定）在空载台架上已经过大**，这个默认值不能再当"安全的起步值"用 | 已定（用户 2026-09-17）：**先把 CSP 做扎实，CST 暂停**；代码/配置/工具保留，不当主线 |
| P10.4 | 控制台台架验证对不上时间轴：主站日志没有时间戳，面板转录里也没有主站模式时间线，所以"某次 [MODE] 切换是不是打字造成的"判不出来。**面板不发目标时主站必然超时回 HOLD，HOLD 的条数本身说明不了问题**——要判的是它有没有落在打字窗口里 | 一次台架运行里能把每一次 [MODE] 切换对齐到面板的哪一步动作（如面板侧记录观察到的主站模式变化及拍号），并覆盖新版点动（按住连续 / 松手停） | 未决（台架那一半被占用的套接字挡住，只跑了离线回归） |

证据：`reports/_panel/evidence_console_round2.txt`；采集脚本 `scripts/bench_panel_drive.sh`、
`scripts/bench_panel_jog.sh`，解析 `scripts/analysis/panel_transcript.py`，
离线回归 `scripts/checks/console_engine_check.py`。

### P11 审计内存与周期路径（2026-09-17 登记）

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P11.2 | 停机序言的 AL 读取在"部分轴已掉出"时是否退化：归档里序言 4 拍跨度 7.014 ms，扣掉那一拍的 3.8 ms 停摆后其余 3 拍仍约 1.07 ms/拍，比正常 1.0 ms 略高；`ecx_readstate` 在广播失败时逐轴退化（每轴一次超时）是已知陷阱（`session_exchange.c:119-144`） | 给出"停机瞬间部分轴已掉出"这一情形下序言各拍的耗时，判定是否有哪一拍吃了逐轴超时 | 未决（P11.1 修好后这一轮的停机五轴都在 OP，取不到该情形；仍只有归档那 1 次样本） |

**P11.1 已解决（2026-09-17 第五轮，判据一/判据二均达成）**：审计数组改成"周期水线 + 停机预留"
两段，解封后的追加路径也用尽只计数、不再分配。台架五轴 180 s：停机入口五轴 AL 8/0、
停机那一拍帧距 998826 ns、整份记录 `mremap` 零次（修改前 3.757 ms 一次）。
**取证过程与两轮前后对照见上面的「P11.1 结案记录」**；索引见 §4。

---

## 3 待决（策略选择，无客观完成判据）

§2 的判据是"做到什么算完成"，这里的判据是"**做决定所需的证据齐没齐**"。
证据齐了就能定，定完再决定要不要落成 §2 里的原子问题。

| # | 待决 | 做决定还缺什么证据 | 现状 |
| --- | --- | --- | --- |
| D1 | **掉出 OP 后怎么办**：重进 OP / 隔离降级 / 整机停 | AL 0x1A 是暂态还是终态；重进 OP 的空转成功率 | 整机停 |
| D2 | **通信判不可用时，发不发尽力而为的停用帧** | 故障态下驱动器还认不认 607A。已证：故障态停机一帧安全输出都没发（`safe_output_sent=false`），但"发了有没有用"没证 | 不发 |
| D3 | **时序判据以谁为准**：主站自算裕量 vs 驱动器同步计数器 | 驱动器记 SM 事件丢失的那一拍，主站裕量读数是多少 | 只用主站 |
| D4 | **越限命令怎么处置**：中止整个会话 vs 只拒这一条（见 P10.2） | 越限在真实命令流里出现的频率与幅度分布；以及"中止整会话"的代价（重启多久、要不要重新上电/回零） | 整会话中止 |
| D5 | **策略偶尔慢于 200 ms 时怎么办**：让主站超时回 HOLD、下一条目标再切回来，还是客户端自己发保持目标 | 刻意制造多次超时（含 >1 s 的停顿）后，位置/跟踪有没有跳变、有没有引发 `MOTION_INVALID`、驱动器 AL 有没有反应 | 让它往返（一次运行里已自然出现 4 次，未观察到危害） |

**D1 追加证据（2026-09-17，AL 0x1A 的存续性）**：一轮运行停机时五轴锁存 0x1A；紧接着的
一次主站启动里轴 1 仍是 20/0x1A（会话没起来，WKC 13、四轴正常）；**再一次启动五轴全部
恢复 8 并成功使能**。⇒ 0x1A 能被后续启动的状态机清掉，但一次启动不保证（目前 1/2）。
"重进 OP 的空转成功率"由此有了第一组数字。

**D1 的背景**：掉出 OP 也可以发生在周期运行期，不必有停机序言参与。触发序列是
"某个周期起过程数据帧异常（WKC 少计，各轴仍在 OP）→ 相关轴的 SM2 事件丢失计数在数毫秒内
从 0 冲到 6 → 驱动器置 AL 0x1A 掉到 SAFE-OP → WKC 进一步少计"。证据见
`reports/_triple_fix/evidence_424s_run.txt` 与 `run_424s_wkc_dropout.log`。

**D3 的背景**："主站算出的裕量为正"和"驱动器记到若干次 SM 事件丢失"可以同时成立 ——
主站的裕量模型没有覆盖驱动器真正使用的判据。这与 P8.4（看门狗寄存器取值）直接相关。

---

## 4 已解决索引

一行一条，为的是让代码注释里的 P 编号永久可查。**P0–P7 的详情与证据见
[`layering-plan-archive.md`](layering-plan-archive.md) 对应节；P8 起的详情在 commit log，
下面的括号里给短哈希。**

| # | 摘要 |
| --- | --- |
| P0.1 | `validate_project.py` 在 HEAD 失败（悬空运动方案引用 + 网口规则缺陷） |
| P0.2 | CI 不跑配置校验 |
| P0.3 | 时序统计只有 min/max |
| P0.4 | 缺 `orangepi-stability-test` 部署配置；`orangepi_bench.json` 引用不存在的运动配置 |
| P0.5 | CSP36° 基线只存在于游离提交（已明确标注作废） |
| P1.1 | 无 `mlockall` |
| P1.2 | 无 SCHED_FIFO / 无 CPU 亲和 |
| P1.3 | 死区超时永久锁存，`deadline_recovery` 无消费者 |
| P1.4 | 无长时运行证据（最长 14.9 秒） |
| P1.5 | 修复 `6c4ea9d` 后无 sync0_margin 数据 |
| P1.6 | CSP 运动可靠性未在有负载情况下长时验证 |
| P2.1 | 客户端断开后主站继续驱动 |
| P2.2 | 生产主站跟随误差保护关闭 |
| P2.3 | 外部目标无限幅无插值 |
| P2.4 | Quick Stop（bit2）/ Halt（bit8）缺失 |
| P2.6 | 运行期 603F 恒为 0 |
| P3.1 | `session_control.c` extern 引用 `main.c` 的全局变量 |
| P3.2 | 编码在发送之后 ⇒ 固有 1 周期延迟 |
| P3.3 | 单位换算散落在工具层 |
| P3.4 | demo 正弦违反 phase-1 禁令且幅值口径错 |
| P4.1 | `ecx_mbxhandler` 完全未用 |
| P4.2 | 缺 `mbx_rl` 记录 ⇒ 无法估算帧时 |
| P4.3 | 电流/电压/温度报告字段恒为 0（死代码） |
| P4.4 | 错误计数器 SOEM 只清零不读回 |
| P4.5 | SOEM 错误环无互斥（RT push / 非 RT pop） |
| P5.2 | 外部目标路径硬要求 `mode==8` |
| P6.1 | 无协议规格文档 |
| P6.2 | 主站就绪信号缺失 |
| P6.3 | 跟随误差限幅硬编码为临时测试值 |
| P6.4 | 单客户端限制和重连语义未文档化 |
| P7.1 | `frame_timeout_us` 修复未 commit 到本地仓库 |
| P7.2 | `功能要求.md` 状态与代码实况不同步 |
| P7.3 | 本地 main 落后 origin/main（原记 18 个提交，2026-09-16 核实只差 1 个） |
| P8.2 | 单周期阻塞预算算清楚并写进报告：诊断读的每轴超时 = (周期 − 帧超时) ÷ 轴数，上限 250 µs、不足 50 µs 则整块跳过。五轴实测 `tail_blocking_budget_ns` = 1000000（= 周期）、每轴 150 µs、额度不足跳过 0 次（`e94da53`） |
| P8.3 | 负同步裕量开始记连续段并可按阈值动作，默认只记账不动作。五轴实测 45 次迟到**全在交换号 1–45**（一次启动段突发），运行期为零——阈值若照搬驱动器的 6，会在每次启动时误触发（`ed84f9c`） |
| P8.4 | 进 OP 后读回 ESC 的 `0x0400`/`0x0420` 进报告。五轴实测 `esc_dl_control=2498`(0x09C2)、`esc_watchdog_pdi=1000`。**这两个值是 ESC 上电默认值**（SOEM 全树零引用），不是我们选的（`52ed8a5`） |
| P9.1 | 删掉"所有 SOEM 访问由会话线程串行执行、周期期间不做邮箱"这句假声明：真正的串行化来自 SOEM 自带的三把 `PTHREAD_PRIO_INHERIT` 锁与按索引分流的收包缓冲（`nicdrv.c:128-132`、`:404-458`）；新增 `soem_call_sites.sh` 防回退检查（`d0c18c5`） |
| P9.2 | 死函数 `emaster_soem_pop_error_safe` 接上取用点（周期每拍 + 停机收尾），错误进报告的 16 条定长环 + `soem_error_count`/`dropped_count`。**环里只有邮箱协议错误**（SDO/SoE abort、意外回帧、紧急报文），不含状态变化与超时。`assignment_failed` 仍就地排空，但那只发生在 PreOP 分配阶段（观测线程还没起），已在函数注释里写清（`754bc00`） |
| P9.3 | 停机失败给原因码 `shutdown_stop_fault`：装配失败／交换前中止／交换失败／超时未到安全态。一次真机失败里读到 `EXCHANGE_FAILED`(`3`)，正常轮次为 `NONE`（`d0531ac`） |
| P9.4 | 观测线程与命令服务器的常态日志退到开关后面（`EMASTER_OBSERVER_VERBOSE`/`EMASTER_CMD_SERVER_VERBOSE`），计数进报告：90 s 一轮日志 7 KB，此前 180 s 是 1.2 MB（`dce7f87`、`7f06c95`） |
| P8.5 | **停机那一拍尾部停摆 4 ms 导致五轴掉出 OP**：根因是停机序言解封审计数组后，第一条记录在正好写满的 84.6 MB 块上触发 `realloc`（内核侧 `mremap` 实测 3.757 ms CPU）。修复即 P11.1。**五轴 180 s 实测**：停机入口五轴 AL 由 20/0x1A 变 **8/0**，停机那一拍帧距由 4025832 ns 变 **998826 ns**，记录里 `mremap` 由 1 次变 **0 次**。取证过程见 §2「P8.5 结案记录」（`19a838e`） |
| P11.1 | **周期路径（含停机序言）里仍会发生分配**：审计数组改成"周期水线（`sealed_capacity`）+ 停机预留（`final_capacity` 硬上限）"两段，解封后的追加路径用尽时只计数（`omitted_final_samples`）不再分配，容量在预留那一刻锁死。离线回归 `tests/unit/audit/test_audit_capacity.c` 断言"总量用尽后 `access_capacity` 原地不动"，两个负对照分别红 9 项／2 项；台架判据同 P8.5。**本轮的坑**：预判漏了"还没预留过"的前提（`0 >= 0`），把会话建立阶段 255 条配置记录当成溢出丢掉，已修并留回归网（`19a838e`） |

---

## 5 与既有文档的关系

- **`phase-1.md`** 的自述状态不可采信（见基线 4.5），已降为历史记录。
- **本文 §2/§3 是未决问题的唯一入口。** `功能要求.md` 讲"做了什么"和证据，
  未决项在那边只留指针与 P 编号，不再各自维护一份。
- **`layering-plan-archive.md`** 是 P0–P7 的完成记录，只读。
- **P 编号是进度口径**：`capability-baseline.md` 给能力基线，本文给"还差什么"。
