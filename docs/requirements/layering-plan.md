# 分层计划与原子问题排序

2026-09-11 建立，2026-09-16 改版为「未决清单」。

依据 `capability-baseline.md`。分层原则：**自底向上，下层不知道上层存在。**
每一层只向下依赖，向上只提供"数据点 + 能力位图 + 时序契约"三件，不提供语义解释。

---

## 0 本文怎么用

老版本停更在 2026-09-14。此后 22 个提交（停机序言收尾、与设备解耦、观测通道、工程化）
一件都没进来。问题不在纪律，在结构：全文 288 行里 30 条有 27 条已完成，完成记录的正文
占了近一半，越写越像日志，于是没人再从头看。2026-09-16 改版定下四条规则：

1. **本文只装未决问题。** §2 是唯一的未决入口。做完的移出 §2，一行进 §4 索引，
   细节正文进 [`layering-plan-archive.md`](layering-plan-archive.md) ——
   完成记录本来就由 commit log 承担，不必在这里复述一遍。
2. **编号只增不复用。** 代码注释直接引用 P 编号（`session_exchange.c` 引 P4.4、
   `session_start.c` 引 P4.3），编号必须永久可查，所以已解决的要留在 §4 索引里。
3. **只有"有客观完成判据"的才进 §2。** 策略选择进 §3「待决」，它的判据是
   "做决定所需的证据齐没齐"。混进 §2 会把"完成判据"列变成空话 ——
   而正是这一列，让 P0–P7 能被一条条判完。
4. **每轮会话收尾时**，把当轮新发现的问题登记进 §2 或 §3。

---

## 1 层级划分

| 层 | 名称 | 职责 | 明确不做 | 时序等级 |
| --- | --- | --- | --- | --- |
| L0 | 主机实时基座 | RT 线程、内存锁定、CPU 隔离、周期时钟、死区判定 | 不知道 EtherCAT | 周期内 |
| L1 | EtherCAT 传输 | 帧收发、FMMU/SM、ESM、DC、WKC、错误计数器、邮箱调度 | 不知道 CiA402，不知道"轴" | 周期内 + 跨周期 |
| L2 | 过程映像与对象访问 | 字节偏移 ↔ 命名数据点；SDO 读写（非 RT 线程） | 不解释数据点含义 | 周期内 + 非实时 |
| L3 | 设备语义（CiA402 轴） | 状态机、控制字/状态字、模式、原始 counts 进出 | **不做单位换算**，不知道减速比 | 周期内 |
| L4 | 运动与安全 | 单位换算、限幅、插值、跟随误差、包线、周期内控制律（PD/阻抗） | 不知道命令从哪来 | 周期内 |
| L5 | 命令与观测接口 | 目标下行、观测上行、失效超时、双缓冲 | **不构造观测帧**，不知道上层是 RL 还是示教 | 跨周期 |
| L6 | 上层应用 | 策略推理、帧构造、任务编排 | 不在本仓库 | 上层自定 |

### 1.1 边界判据

一条功能属于哪层，看它**需要知道什么**：

- 需要知道减速比/编码器分辨率 → L4 及以上（L3 只搬 counts）
- 需要知道"这是一条腿的髋关节" → L6
- 需要知道帧格式/传输方式 → L5 以上
- 需要知道 SM/FMMU → L1

### 1.2 L4 必须承载周期内控制律

这不是设计偏好，是硬件逼出来的：`MIT_Control`（kp/kd + 前馈力矩）在 EtherCAT ESI 中
无对应对象（见基线 2.4）。若上层需要关节阻抗，PD 环只能放在 L4 按周期跑，
输出折算为 CST 力矩或 CSP 位置。L4 因此不是纯粹的"限幅层"。

### 1.3 L5 的形态由时序反推，不预设

已知量：EtherCAT 周期 1ms；典型策略频率 50-100Hz（rl_sar `config.yaml` 量级）。
⇒ 周期比在 10:1 到 20:1，**延迟不是瓶颈**。

真正的约束只有两条：

1. RT 周期**绝不能阻塞等待** L5 ⇒ 排除同步请求-响应
2. 上层命令缺失或迟到时必须安全降级 ⇒ 必须有失效超时（当前已实现）

满足这两条的实现：Unix 域套接字 + 互斥保护双缓冲（已实现）。

观测量按可得性分两档（硬件决定，非设计选择）：

- **周期档**：位置、速度、力矩（在 TxPDO 内，见基线 2.2）
- **慢速档**：电流、电压、温度（只能 SDO，约 3 周期 + 抖动，见基线 2.3）
  ⇒ 10-100ms 级轮询，错开轴号

IMU 不在 EtherCAT 链路上，本轮不纳入。

---

## 2 未决的原子问题

### P8 运行期时序与可靠性

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P8.1 | **整帧未回**：运行期每隔约 45 秒有 1–3 个周期收不到任何回帧。**已定性为超时偏紧造成的误报为主**（250 µs 固定超时 vs 实测成功回程上限 170–390 µs，见下）；是否还另有真丢帧未定 | 连续 ≥1 小时，`wkc_no_frame_count` 的**每个**事件都用非超时证据归因（发送迟到 / ESC 计数器 / AL 状态 / DC 裕量），且期间无掉出 OP（AL 全程 8）；报告须同时给出 `tail_max_receive_ok_ns`，使"其中多少是 250 µs 偏紧造成的误报"可估。**不要求计数为 0**——超时固定在 250 µs 而成功回程实测到 390 µs，为 0 是个不可达且无意义的门槛 | 未决；超时已固定，误报成分已知，待一小时归因 |
| P8.2 | **单周期内的阻塞超时预算仍可能超过周期** | 把收包／邮箱／FPRD 三段的各自耗时刻进报告（亚步骤计时），并证明最坏情况 ≤ 周期；或把 FPRD 诊断段移出周期尾部 | 未决 |
| P8.3 | **`sync0_margin_ns < 0` 时周期回路无反应**：主站明知本周期输出对驱动器已经迟到，仍照常推进 | 定下负裕量时的动作（跳过本周期输出／记录并继续／触发降级）并实现，且该动作在报告里可见 | 未决 |
| P8.4 | **看门狗寄存器从未配置**：`0x0400`（分频）/`0x0420`（SM 看门狗时间）在全树零引用 | 读回实际生效值并与驱动器手册核对；若与主站时序不匹配则显式配置并留实测证据 | 未决 |

**P8.1 已有结论（2026-09-17）**

现象计在 `wkc_no_frame_count` 上，与"帧回来了但 WKC 少计"的 `wkc_mismatch_count` 分开。
四项排除已经做完：

- **与观测通道无关**：8 臂隔离试验里，观测通道全关的 `base` 臂反而最多（3、3 次），
  开通道但一个客户端都没有的 `obsnone` 臂是 0、0、4 次。此前把故障归因于观测通道的判断
  **已撤回**。
- **不是链路问题**：5 个从站的 ESC 错误计数器全程为 0（`rx` / `forwarded_rx` / `epu` / `pdi`）；
  首次不符现场快照里帧距 1.0005 ms（发帧准点）、轴仍在 AL state=8（没有先掉出）。
- **不是发送晚**：8 臂的 `max_send_lateness` 全程 ≤55 µs（最大 55488 ns），发送侧不构成嫌疑。
- **主因是超时压在回程分布尾部**——不是"帧没回来"，是"回来得比超时晚"。见下。

判定主因用的 8 臂数据（五轴，各 45 s，前 4 臂超时 250 µs、后 4 臂 600 µs）：

| | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `wkc_no_frame_count` | 3 | 1 | 1 | 3 | 2 | 1 | 0 | 0 |
| `max_send_lateness` (ns) | 1323 | 25795 | 23956 | 26056 | 21230 | 3689 | 35231 | 55488 |
| `tail_max_receive_ns` | 267893 | 268814 | 274054 | 268974 | **645525** | **644194** | **222045** | **178196** |
| `sync0_late_count` | 16 | 57 | 15 | 67 | 84 | 0 | 0 | 0 |

三条读数：

1. **未回周期的收包段时长恒等于超时值本身**（250 组 = 268–274 µs ≈ 250 + 收尾开销；
   600 组的失败臂 = 645/644 µs ≈ 600 + 收尾开销）。这是"等待被超时截断"的指纹，
   不是"帧丢了"。
2. **干净臂的收包段上限只有 178–222 µs**，而超时是 250 µs —— 余量仅 12%。
   超时从一开始就压在分布的尾部上。
3. 超时 250 → 600 µs 后计数 8 → 3，是剂量-反应。

**修复尝试与撤回（2026-09-17）**

先按"超时压在尾部"改了（提交 `aafff14`）：`frame_timeout_us` 由实测 `tail_max_receive_ok_ns × 2`
导出、钳在 `[cycle_ns/4, cycle_ns/2]`；FPRD 诊断读拆出独立的 `fprd_timeout_us`；标定样本
只收 `actual_wkc > 0` 的周期。**取值方式当日撤回**（`fprd_timeout_us` 的拆分保留）。理由：

实测上限是**全程最大值、只涨不落**，而超时又等于它的 2 倍。于是一个大样本就把超时顶上去，
此后的慢帧全被判成"回来了"——`wkc_no_frame_count` 被它自己压成 0。**这不是链路变好，是把
尺子放宽**；拿这个数字当判据，判据就是不可证伪的。

收手的判断依据：掉线主因已在别处定位并修好（停机序言），这些未回事件从未引发掉线。
肥尾按"非 PREEMPT_RT 内核 + 台架共存负载的固有属性"记录，不再试图用超时盖掉。
`tail_max_receive_ok_ns` / `tail_max_receive_ns` 的测量与上报**保留**——超时不动了，
"实测离超时还有多远"才是一个有意义的比较。

**5 臂实测（五轴，各 45 s，`base`）**

| 臂 | `tail_max_receive_ok_ns` | `frame_timeout_us`（自适应版） | 撞 `cycle_ns/2` | `sync0_late_count` | `wkc_no_frame_count` |
| --- | --- | --- | --- | --- | --- |
| 1 | 390 µs | 500 | 是 | 2 | 0 |
| 2 | 311 µs | 500 | 是 | 1 | 0 |
| 3 | 171 µs | 340 | 否 | 63 | 0 |
| 4 | 181 µs | 360 | 否 | 0 | 0 |
| 冒烟 | 174 µs | 346 | 否 | 47/48 | 0 |

**由此确认的两件事**

1. **`sync0_late_count` 与尾部不相关，上面那条候选解释不成立。** 回程上限最小的臂
   （171 µs）迟到 63 次，上限最大的臂（390 µs）迟到 2 次——方向与"5 轴同时 SYNC0 压住
   ESC 转发造成肥尾"相反。原先的依据（"有未回的臂 84/67/57/16、干净臂 0/0"）在新数据下
   不成立：干净臂的迟到计数可以是 0、1、2、47、63，与未回计数无关。
2. **报告里的 `round_trip_ns` 分布不能用来判超时余量。** 它从发帧算到 3×FPRD 之后
   （`session_exchange.c:495` 的 `receive_end`），量的是**整个周期尾部**（收包 + 邮箱 +
   诊断读），不是帧的回程。判余量要看 `tail_max_receive_ok_ns`（纯收包段，
   `session_exchange.c:412`）。8 臂那会儿用 `tail_max_receive_ns` 是对的，没有踩这个坑。

**已知读数缺陷（不是新问题，是重新确认的旧问题）**：`cycle_ns/4` = 250 µs 确实偏紧
（实测成功回程上限 170–390 µs），所以 `wkc_no_frame_count` 里**含一个误报成分**——那些帧
回来了，只是回得比 250 µs 晚。判读该计数时必须扣掉这一层，不能直接当成"帧丢失"。
是否还另有真丢帧，是 P8.1 剩余待分辨的部分。

**P8.2 的现状**：抽样只保证这套阻塞预算不再是**每周期**、且不再与丢帧叠加，没有消除它本身。
周期上收包（`frame_timeout_us` = `cycle_ns/4` 固定）+ 邮箱推进 + 3×`ecx_FPRD(0x0300)`
（`fprd_timeout_us` = `cycle_ns/4` 固定）仍可能落在同一周期里。唯一的变化是两者已拆成独立
常量（此前共用一个值），任一方调整不再牵动另一方的全失败最坏情况。最坏情况本身仍未证明
≤ 周期；`cycle_ns/4` 这个固定值是**故障侧预算**（帧真丢时最多赔 1/4 周期，留空间给
recovery），与"正常时够不够等"是两回事。

**P8.4 的现状**：全树唯一的 `0x0400` 命中是 `src/cia402/controller.c:87` 的状态字 bit10
（target reached），与 ESC 寄存器无关。驱动器判"帧没按时到"用的就是这两个寄存器，
它们的取值直接决定 AL 0x001A 的门槛 —— 现在这个门槛是 SOEM 默认值，不是我们选的，
也从没读回核对过。

### P9 架构硬约束与仪表缺口

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P9.1 | **SOEM 访问串行化的声明与实现矛盾**：`session_internal.h:21` 写明"所有 SOEM 访问均由会话线程串行执行，周期期间不进行邮箱访问"，但 P4.3 的观察线程与周期线程共用同一个 SOEM context/socket 做邮箱往返 | 声明与实现一致（把观察线程的 SOEM 调用收归会话线程，或改掉声明并证明共用安全），且有一条可执行检查防止回退 | 未决 |
| P9.2 | **`emaster_soem_pop_error_safe` 无调用者**：SOEM 自己的错误环（状态变化、超时，**带时间戳**）从未被取用，等于放弃了总线侧唯一带时序的记录 | 错误环被消费并进报告，或明确判定无用并删除该函数 | 未决 |
| P9.3 | **`stop_process_data` 返回 false 时不记原因**：是交换失败还是驱动器始终未到安全停止态，无法区分 | 两种原因各有独立标记，且在一次真机失败里可见 | 未决 |
| P9.4 | **观测线程 stderr 常态刷屏**：启动若干行 + 每轴第 1~3 次读取明细 + `[P4.4]` 首周期错误计数器仍在写；一次 424 s 运行的日志 1.4 MB，相当部分是这些行 | 常开路径不写 stderr（改走报告字段或降级日志级别），424 s 运行的日志量级可接受 | 未决 |

**P9.1 的现状**：阶段 2.4 摘掉的是 `observer_mutex`，没有触及这条约束。它仍是丢帧的
候选解释之一（一轮 1/39835），但报告仍无亚步骤计时，无法区分链路抖动、主机调度停顿
与 socket 争用 —— 这与 P8.2 是同一块仪表缺口。

**P9.3 的现状**：2026-09-15 的 `safe_output_sent` 口径修复分担了一半 —— 它现在严格表示
"帧发没发出去"，所以"发了但未确认"与"根本没发"可以分开；"驱动器始终未到安全停止态"
仍无独立标记。

**P9.4 的现状**：每轮一条的 `[P4.3] 循环迭代开始` 已于 2026-09-14（`664deb1`）去掉，
其余仍在。

### P2 / P5 的未决项（编号不变，承接 archive）

| # | 问题 | 完成判据 | 状态 |
| --- | --- | --- | --- |
| P2.5 | Fault Reset 调用路径已存在但未真机验证 | `Fault → Fault Reset → Switch On Disabled` 全程可走 | 已实现·未验证（硬件阻塞） |
| P5.1 | 运行期模式切换未实现（模式在计划构建期固定） | 0x119800 方案下 OP 态切换 CSP/CSV/CST | 未做（CSP 专项不要求） |
| P5.3 | `fixed_csv` 的 1A02 内容 ESI 与手册冲突，未验证 | 真机读回确认 | 硬件阻塞（CSP 专项不要求） |
| P5.4 | ~~无 3 轴及以上时序数据~~ **措辞已过时** | 逐步扩到目标轴数，每档留报告 | **待重判**：五轴配置已入库（`8e69b64`）、bench 脚本默认已改五轴，这条不能再挂"硬件阻塞" |

**P2.5 硬件阻塞原因**：驱动器静置后位置反馈正常但不执行 607A，断电重启恢复
（见 [[drive-idle-no-response]]）。

---

## 3 待决（策略选择，无客观完成判据）

§2 的判据是"做到什么算完成"，这里的判据是"**做决定所需的证据齐没齐**"。
证据齐了就能定，定完再决定要不要落成 §2 里的原子问题。

| # | 待决 | 做决定还缺什么证据 | 现状 |
| --- | --- | --- | --- |
| D1 | **掉出 OP 后怎么办**：重进 OP / 隔离降级 / 整机停 | AL 0x1A 是暂态还是终态；重进 OP 的空转成功率 | 整机停 |
| D2 | **通信判不可用时，发不发尽力而为的停用帧** | 故障态下驱动器还认不认 607A。已证：故障态停机一帧安全输出都没发（`safe_output_sent=false`），但"发了有没有用"没证 | 不发 |
| D3 | **时序判据以谁为准**：主站自算裕量 vs 驱动器同步计数器 | 驱动器记 SM 事件丢失的那一拍，主站裕量读数是多少 | 只用主站 |

**D1 的背景**：掉出 OP 也可以发生在周期运行期，不必有停机序言参与。触发序列是
"某个周期起过程数据帧异常（WKC 少计，各轴仍在 OP）→ 相关轴的 SM2 事件丢失计数在数毫秒内
从 0 冲到 6 → 驱动器置 AL 0x1A 掉到 SAFE-OP → WKC 进一步少计"。证据见
`reports/_triple_fix/evidence_424s_run.txt` 与 `run_424s_wkc_dropout.log`。

**D3 的背景**："主站算出的裕量为正"和"驱动器记到若干次 SM 事件丢失"可以同时成立 ——
主站的裕量模型没有覆盖驱动器真正使用的判据。这与 P8.4（看门狗寄存器取值）直接相关。

---

## 4 已解决索引

一行一条，为的是让代码注释里的 P 编号永久可查。**详情与证据见
[`layering-plan-archive.md`](layering-plan-archive.md) 对应节。**

| # | 摘要 |
| --- | --- |
| P0.1 | `validate_project.py` 在 HEAD 失败（悬空运动方案引用 + 网口规则缺陷） |
| P0.2 | CI 不跑配置校验 |
| P0.3 | 时序统计只有 min/max |
| P0.4 | 缺 `orangepi-stability-test` 部署配置；`orangepi_bench.json` 引用不存在的运动配置 |
| P0.5 | CSP36° 基线只存在于游离提交（已明确标注作废） |
| P1.1 | 无 `mlockall` |
| P1.2 | 无 SCHED_FIFO / 无 CPU 亲和 |
| P1.3 | 死区超时永久锁存，`deadline_recovery` 无消费者 |
| P1.4 | 无长时运行证据（最长 14.9 秒） |
| P1.5 | 修复 `6c4ea9d` 后无 sync0_margin 数据 |
| P1.6 | CSP 运动可靠性未在有负载情况下长时验证 |
| P2.1 | 客户端断开后主站继续驱动 |
| P2.2 | 生产主站跟随误差保护关闭 |
| P2.3 | 外部目标无限幅无插值 |
| P2.4 | Quick Stop（bit2）/ Halt（bit8）缺失 |
| P2.6 | 运行期 603F 恒为 0 |
| P3.1 | `session_control.c` extern 引用 `main.c` 的全局变量 |
| P3.2 | 编码在发送之后 ⇒ 固有 1 周期延迟 |
| P3.3 | 单位换算散落在工具层 |
| P3.4 | demo 正弦违反 phase-1 禁令且幅值口径错 |
| P4.1 | `ecx_mbxhandler` 完全未用 |
| P4.2 | 缺 `mbx_rl` 记录 ⇒ 无法估算帧时 |
| P4.3 | 电流/电压/温度报告字段恒为 0（死代码） |
| P4.4 | 错误计数器 SOEM 只清零不读回 |
| P4.5 | SOEM 错误环无互斥（RT push / 非 RT pop） |
| P5.2 | 外部目标路径硬要求 `mode==8` |
| P6.1 | 无协议规格文档 |
| P6.2 | 主站就绪信号缺失 |
| P6.3 | 跟随误差限幅硬编码为临时测试值 |
| P6.4 | 单客户端限制和重连语义未文档化 |
| P7.1 | `frame_timeout_us` 修复未 commit 到本地仓库 |
| P7.2 | `功能要求.md` 状态与代码实况不同步 |
| P7.3 | 本地 main 落后 origin/main（原记 18 个提交，2026-09-16 核实只差 1 个） |

---

## 5 与既有文档的关系

- **`phase-1.md`** 的自述状态不可采信（见基线 4.5），已降为历史记录。
- **本文 §2/§3 是未决问题的唯一入口。** `功能要求.md` 讲"做了什么"和证据，
  未决项在那边只留指针与 P 编号，不再各自维护一份。
- **`layering-plan-archive.md`** 是 P0–P7 的完成记录，只读。
- **P 编号是进度口径**：`capability-baseline.md` 给能力基线，本文给"还差什么"。
